- All types & interfaces should be re-exported from index.ts
- DO NOT use prefix; 'T' for types and 'I' for interfaces
- Use CapitalCase for interfaces & types
- For e.g. User, UserToken, Topic, Category.
